
# worktrees + local claude state
.claude/

# dependencies — restored by `npm ci` from package-lock.json, never committed
node_modules/

# build output — regenerated by `npm run build`, deployed from CI
dist/
